// Функция проверки чётности числа
export const isEven = (n) => n % 2 === 0;
/*
Оператор % (остаток от деления) возвращает остаток от деления числа на 2
Если остаток равен 0 => число чётное => возвращаем true
Если остаток равен 1 => число нечётное => возвращаем false
*/
